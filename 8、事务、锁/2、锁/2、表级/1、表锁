对表进行读操作时加的锁（是共享型锁）：
    lock tables table_name read;
对表进行写操作时加的锁（是独占型锁）：
    lock tables table_name write;

*注意*：表锁除了会限制别的线程的读写操作外，也会限制本线程接下来的读写操作！   （线程即事务？）

释放表锁（若会话断开，也会随之自动释放所有表锁）：
    unlock tables;                  //会释放当前会话的所有表锁


*注意*：尽量避免在使用 InnoDB 引擎的表使用表锁，因为表锁的颗粒度太大，会影响并发性能。

（完）