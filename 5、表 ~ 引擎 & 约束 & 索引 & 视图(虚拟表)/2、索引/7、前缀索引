前缀索引的创建和优缺点？

CREATE  INDEX 索引名k_name  ON `表名tab_name` (列名f1 [(索引长度l)] [ASC | DESC]);

优点：
    1、在一些大字符串的字段作为索引时，可以使用该字段的前几个字符建立索引来减小索引项的大小，有效提高索引的查询速度

缺点：
    1、order by 就无法使用前缀索引
    2、无法把前缀索引用作覆盖索引

