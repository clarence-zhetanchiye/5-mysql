逻辑运算符有：
运算符                 逻辑含义
AND（也可写作&&）         与
OR（也可写作||）          或
NOT（也可写作！）         非
XOR                     亦或

逻辑运算符中的 AND 和 OR 和 NOT 常被用于sql查询语句中的WHERE条件，和WHERE条件是平级的，这是主要应该掌握的。其他的详见《MySQL从入门到精通》

（完）