比较运算符，是用于数据的比较的。
（在sql查询语句的WHERE条件里用的那些，就是以下这些比较运算符；在sql查询语句中和WHERE条件平级的逻辑运算符还有AND、OR、NOT）

符号                含义                                 返回值                                 备注

=           用来判断数字、字符串、表达式是否相等             相等则返回1，否则返回0              MySQL是根据等号两边内容的ASCII是否
            （注意NULL不能用 = 来判断 ）                                                   相等来判断的。

!=或<>      用来判断数字、字符串、表达式是否不相等           不相等则返回1，否则返回0             MySQL是根据等号两边内容的ASCII是否
            （注意NULL不能用 != 或 <> 来判断）                                             不相等来判断的。

>           用来判断数字的大小关系                         大于则返回1，否则返回0              只用于判断数字的大小关系
            （注意NULL不能用 > 来判断）

>=          ...同上...                                   ...同上...                       ...同上...

<           ...同上...                                   ...同上...                       ...同上...

<=          ...同上...                                   ...同上...                        ...同上...

BETWEEN AND   对于 x BETWEEN a AND b ,判断x是            在范围内则返回1，否则返回0          只用于判断数字
              否在 a <= x <= b 的范围内。

IN          对于 x IN (a,b,···) ，判断x是否存在            存在于其中则返回1，否则返回0        可以用于多种数据类型
            于 a,b,··· 中
NOT IN

IS NULL     专门用于判断是否为空值                         是空值则返回1，否则返回0            只用于判断是否是空值NULL

IS NOT NULL 专门用于判断是否是空值                         不是空值则返回1，否则返回0          只用于判断是否是空值NULL

LIKE        对于 a LIKE 'x' ，判断a与模糊匹配x是            匹配则返回1，否则返回0             只能用于字符串
            否匹配，模糊匹配x中常含有符号%
NOT LIKE

REGEXP      对于 a REGEXP 'reg' ，判断a与正则表            匹配则返回1，否则返回0              只能用于字符串
            达式reg是否匹配

（完）